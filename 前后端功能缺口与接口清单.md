# Audio Dialogue Studio：功能缺口与后端接口清单

> 审计日期：2026-09-22  
> 范围：当前 Vue 前端实际调用的接口、`FRONTEND_API.md` 及已实现页面。本文为静态审计，未连接运行中的后端验证接口可达性。

## 结论

当前系统已经具备项目、正式分段、分析、音频任务、角色、Provider/模型和导出的基础页面与前端调用层。要形成稳定可交付的完整流程，后端优先需要保证以下三项：

1. **两种分段模式有明确、可持久化的语义。** 剪映 TTS 必须走本地规则切分；外部 AI 音频必须走指定 LLM 分析，并都写入正式 `DialogueSegment`。
2. **所有 Long/雪花 ID 均返回 JSON 字符串。** 不能返回 JSON number，否则浏览器会丢失精度。
3. **任务、分析、导出的历史可以按项目重新查询。** 否则刷新页面或重新进入项目后，用户会丢失进行中的任务与下载记录。

## 当前端到端流程

| 场景 | 当前前端行为 | 依赖的后端能力 | 现状/风险 |
| --- | --- | --- | --- |
| 项目列表 | 进入 `/projects` 请求项目列表 | `GET /projects/query` | 已接入；必须返回数组，即使为空也应返回 `[]`。 |
| 打开工作台 | 请求项目、角色、正式分段 | `GET /projects/{id}`、`GET /projects/{id}/characters`、`GET /projects/{id}/segments` | 已接入；新项目的分段/角色接口必须返回空数组，而非 404。 |
| 剪映 TTS 分段 | 点击“生成分段”，不传 LLM Provider/Model，成功后自动应用结果 | 分析创建、分析应用、本地规则切分 | 前端已接入该约定；后端需要实现/保证本地规则分段。 |
| 外部 AI 音频分段 | 选择当前第一个启用的 LLM Provider/Model，创建分析后进入预览确认 | LLM 分析、分析查询、应用结果 | 已接入；当前没有让用户选择 Skill/LLM 的界面。 |
| 音频生成 | 为当前正式分段创建任务并轮询当前任务 | 音频任务创建、详情、取消、重试、资产 URL | 当前页面内可用；刷新后任务记录会丢失。 |
| 导出 | 创建 SRT/素材包，并在当前页面轮询 | 导出创建、详情、下载 | 当前页面内可用；刷新后导出记录会丢失。 |

## P0：上线前必须确认或提供的后端能力

### 1. 分段模式的服务端契约

当前前端按下面的约定调用现有分析接口：

| 模式 | 调用 | 预期服务端行为 |
| --- | --- | --- |
| `JIAN_YING_TTS` | `POST /projects/{projectId}/analysis-runs`，仅传 `skillVersionId`、`rewriteMode` | 使用本地代码/规则按标点、换行、长度等规则切分 `originalDialogue`；不得调用 LLM；返回 `SUCCEEDED` 的分析运行。 |
| `EXTERNAL_AUDIO` | 同一接口，同时传 `providerId`、`modelId` | 调用指定的 LLM，返回候选分段和改写/情绪等结果。 |
| 两种模式的落库 | `POST /projects/{projectId}/analysis-runs/{runId}/apply` | 原子替换该项目的有效正式分段，返回完整 `DialogueSegment[]`。 |

必须满足：

- 本地规则分段在没有可用 LLM 时仍可运行。
- 当前前端仍会选择一个已发布 Skill 并传入 `skillVersionId`。若本地规则分段不应依赖 Skill，后端需要提供默认规则版本，或采用上文建议的独立生成接口。
- 新项目调用 `GET /projects/{id}/segments` 返回 `[]`；规则/AI 分段应用后返回按 `segmentNo` 升序的完整数组。
- 分段的 `originalText`、`spokenText`、`subtitleText`、`sourceStart`、`sourceEnd`、`version` 必须完整写入，确保编辑、拆分、排序和音频生成可用。
- 应用结果必须使用项目乐观锁版本；冲突时返回明确的 `409` 业务错误。

如果不希望复用“分析运行”实现剪映规则分段，建议新增更明确的接口：

```http
POST /api/v1/projects/{projectId}/segments/generate
Content-Type: application/json

{
  "mode": "LOCAL_RULES",
  "projectVersion": 1,
  "rulesVersion": "default"
}
```

响应可直接返回 `DialogueSegment[]`，或返回可轮询的 `SegmentGenerationRun`。二选一即可；不建议前端本地切割后仅在内存展示，因为后续编辑、音频任务、导出都依赖数据库中的正式分段。

### 2. 统一 ID 与响应结构

后端所有 JSON 响应均需遵守：

```json
{
  "success": true,
  "code": "OK",
  "message": "success",
  "data": {},
  "traceId": "...",
  "timestamp": 1780000000000
}
```

- `id`、`projectId`、`providerId`、`modelId`、`skillVersionId`、`createdBy`、`updatedBy` 等所有 Long ID 均为字符串，例如 `"2101842964950081500"`。
- 时间使用毫秒时间戳；空集合使用 `[]`，而不是 `null` 或 404。
- `GET /projects/query` 返回 `AdsProject[]`；`GET /projects/{id}` 至少返回项目字段。前端当前以独立的 `GET /projects/{id}/segments` 获取正式分段，因此详情接口不再强依赖内嵌 `segments`。

### 3. 必须实际可用的既有接口

以下接口已有前端调用，后端需实现并在测试环境可用：

| 领域 | 接口 |
| --- | --- |
| 项目 | `POST /projects`、`GET /projects/query`、`GET /projects/{id}`、`PUT /projects/{id}` |
| 分段 | `GET /projects/{id}/segments`、`PUT /segments/{id}`、`POST /segments/{id}/split`、`POST /segments/merge`、`POST /projects/{id}/segments/reorder` |
| 分析 | `POST /projects/{id}/analysis-runs`、`GET /projects/{id}/analysis-runs/{runId}`、`POST /projects/{id}/analysis-runs/{runId}/apply` |
| AI/音频配置 | `GET /audio/providers`、`GET /skills`、`GET /skills/{id}/versions` |
| 音频任务 | `POST /projects/{id}/audio-tasks`、`GET /audio-tasks/{id}`、取消/重试/音频资产 URL 接口 |
| 角色与导出 | 当前 `FRONTEND_API.md` 列出的角色 CRUD、导出创建/查询/下载接口 |

## P1：建议后端新增的接口

### 1. 项目下的任务与导出历史

当前前端只保存本次会话刚创建的任务/导出 ID。刷新页面、切换路由或重新打开项目后，历史记录消失。

建议提供：

```http
GET /api/v1/projects/{projectId}/audio-tasks?cursor=&limit=20
GET /api/v1/projects/{projectId}/exports?cursor=&limit=20
GET /api/v1/projects/{projectId}/analysis-runs?cursor=&limit=20
```

每项返回分页结构：

```json
{ "items": [], "nextCursor": null, "hasMore": false }
```

这三项可让前端恢复轮询、展示失败原因、重新下载历史产物，并允许用户重新应用未应用的 AI 分析结果。

### 2. 项目生命周期：删除、回收站与恢复

`AdsProject` 已有 `deletedAt` 和 `deleted` 字段，但当前 API/前端没有删除和恢复操作。建议提供：

```http
DELETE /api/v1/projects/{id}?version={version}
POST   /api/v1/projects/{id}/restore
GET    /api/v1/projects/trash?cursor=&limit=20
```

并明确普通项目列表是否默认排除逻辑删除记录。

### 3. 本地规则分段的可配置性

若剪映 TTS 需要稳定适配字幕长度，建议让后端规则接口或项目配置支持：

- 每段最大字符数、每行最大字符数；
- 按中文/英文标点、换行、角色名切分的开关；
- 最短段合并策略；
- 规则版本和实际使用的规则快照。

这能保证同一项目可复现地重新分段，也便于问题排查。

## 当前缺失的前端功能（不要求新增后端接口）

| 功能 | 说明 | 优先级 |
| --- | --- | --- |
| LLM/Skill 显式选择 | 现在 AI 分段自动选择第一个启用的 LLM Provider/模型和第一个已发布 Skill；应让用户明确选择。 | P1 |
| 分析自动轮询 | 分析页目前需要手动“刷新状态”；现有查询接口已足够，前端可自动轮询至终态。 | P1 |
| 任务/导出历史页 | 需要配合上文的项目级列表接口。 | P1 |
| 请求中的加载/禁用态 | 分段应用、保存、删除、导出等异步操作应禁用重复点击，并显示后端错误码。 | P1 |
| 分段规则预览 | 剪映 TTS 在正式写入前可显示规则切分预览，允许人工调整。若采用直接生成接口，需要后端提供预览或可撤销版本。 | P2 |
| 权限与审计 | 当前文档写明未实现鉴权；生产环境需身份、项目归属校验、操作审计。 | P0（生产环境） |

## 建议验收用例

1. 创建 `JIAN_YING_TTS` 项目，录入多段原始台词，点击“生成分段”；确认没有 LLM 调用，且工作台立即出现持久化的正式分段。
2. 创建 `EXTERNAL_AUDIO` 项目，使用指定 LLM；确认分析预览可查看、应用后正式分段可编辑。
3. 用 19 位 ID 创建并重新打开项目；确认 URL、角色、分段、Provider/模型关联 ID 均未改变。
4. 在正式分段上编辑、拆分、合并和排序；确认版本冲突返回 409，刷新后数据一致。
5. 创建音频任务与导出，刷新页面后仍能从项目历史中看到状态、失败原因、试听与下载入口。
6. 删除项目、进入回收站、恢复项目；确认项目及其分段/任务数据的保留策略符合产品定义。
